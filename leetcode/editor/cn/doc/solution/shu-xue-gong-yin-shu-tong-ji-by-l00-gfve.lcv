> [172. 阶乘后的零](https://leetcode.cn/problems/factorial-trailing-zeroes/description/)

# 思路

`0` 或者说 `10` 的倍数只能由 `5` 和 `2` 结合产生，因为 `10` 的质因数就只要 `5` 和 `2`，所以无论什么数字相乘，如果这些数字里面包含了 `5` 和 `2` 就一定会产生尾数 `0`，说人话就是 “乘以10”；

所以只需要找出 `n!` 内有多少个 `2` 有多少个 `5` 就可以知道答案了，这里稍微思考就能发现，`2` 的数量必然远大于 `5`，也就是说有 `2` 未必有 `5` ，有 `5` 必然有 `2`，所以只统计 `5` 的数量就可以了，有多少个 `5` 等于至少有多少个 `2` 也就至少有多少个 `0`；

那么有多少个 `5` 这个问题有可以细分为两个思路解决，但实际内核都一样，就是 5 进制：

1. 寻找 `n` 以内有多少个 `5` 能覆盖的位置，那么覆盖的位置必然至少有 `1个5`，然后寻咋 `n` 以内有多少个 `25` 能覆盖的位置，这些位置必然至少有 `2个5`，寻找 `n` 以内有多少个 $5^x$ 的位置，这些位置必然有 `x个5`，而因为是递增搜索，每次 +1 即可；

2. 把方法一直接看做 5 进制数位统计，整除 5 的到答案计入统计即可；

复杂度： $O(log_5^{n})$

* []

```Python
# class Solution:
#     def trailingZeroes(self, n: int) -> int:
#         cnt = 0
#         nums5 = 5
#         while n >= nums5:
#             cnt += n // nums5
#             nums5 *= 5
#         return cnt
        
# 或

class Solution:
    def trailingZeroes(self, n: int) -> int:
        cnt = 0
        while n :
            n //= 5
            cnt += n
        return cnt
        

```

