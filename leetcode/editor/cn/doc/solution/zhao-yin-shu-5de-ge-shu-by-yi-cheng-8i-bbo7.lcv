# 思路

可以知道$10=2 * 5$,那么就是找$2$和$5$因数中更小的一个，可以看到5的间隔更大，直觉上来说找到5的个数就可以了。

问题上转换为求$[1...n]$中因数$5$的个数。
- 我们知道有因数5的个数为: $n \ // \ 5$，因为每5个数会出现一次5，比如$1,2,3,4,5,6....10...15$，个数记为$c1$
- 由于我们找到了有1个5因数的个数，这个时候还需要去找有因数$25$的个数为：$n \ // \  25$,其实也就是从为有因数$5$的数中每$5$个取一个，个数记为$c2$，那么$c2 = c1 \ // \ 5$
- 类似的，有因数$125$的数，记为$c3$,那么$c3 = c2 \ // \ 5$

那么我们就可以写一个循环，不断加上$n // 5$，即可得到我们想要的结果。

这一题相同，可见此题：[双倍经验](https://atcoder.jp/contests/abc148/tasks/abc148_e)

# Code

* []

```python
class Solution:
    def trailingZeroes(self, n: int) -> int:
        res = 0
        while n:
            res += n // 5
            n = n // 5
        return res
```

这道题数据范围很小，其实也可以暴力：

* []

```python
class Solution:
    def trailingZeroes(self, n: int) -> int:
        cnt5 = 0
        for i in range(1,n + 1):
            x = i 
            while x % 5 == 0:
               cnt5 += 1
               x //= 5
        return cnt5

```

